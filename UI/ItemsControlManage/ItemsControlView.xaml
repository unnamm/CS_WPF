<UserControl x:Class="UI.ItemsControlManage.ItemsControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.ItemsControlManage"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--panel template-->
        <ItemsControl Grid.Column="0" ItemsSource="{Binding Datas}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="9" Columns="6"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Background="SkyBlue" Margin="2">
                        <TextBlock Text="{Binding}" FontSize="30"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ScrollViewer Grid.Column="1">
            <ItemsControl ItemsSource="{Binding Datas}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Background="SkyBlue" Margin="2">
                            <TextBlock Text="{Binding}" FontSize="30"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!--listview-->
        <Grid Grid.Column="2">
            <ListView ItemsSource="{Binding Datas}" ForceCursor="False" Focusable="False"/>
        </Grid>

        <!--listbox-->
        <Grid Grid.Column="3">
            <ListBox ItemsSource="{Binding Datas}" ForceCursor="False" Focusable="False">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="30"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

    </Grid>
</UserControl>
