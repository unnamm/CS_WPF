<Page x:Class="UI.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:UI"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <StackPanel Margin="20">
        <TextBlock Text="home" Margin="0,0,0,12"/>
        <ui:Button Content="이미지 선택" Command="{Binding SelectImageCommand}" HorizontalAlignment="Left"/>
        <Image Source="{Binding Image}" MaxWidth="400" MaxHeight="400" Margin="0,12,0,0"
               Stretch="Uniform" HorizontalAlignment="Left"/>
    </StackPanel>
</Page>
