namespace Database
{
    /// <summary>
    /// RolePermissions.PermissionCode에 저장되는 값 (DB에 별도 마스터 테이블 없이 코드로 관리)
    /// </summary>
    public enum Permission
    {
        EmployeeManage,
        PermissionManage,
    }
}
